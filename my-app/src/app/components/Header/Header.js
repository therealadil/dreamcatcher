import React from 'react';
import './Header.css';

const Header = () => {
    return (
        <header className="header">
            <h1>🌙 DreamCatcher</h1>
            <button className='button'>👤</button>
        </header>
    );
};

export default Header;

